// Contains the application code for the pokedex 

// Create a Vue app
const app = Vue.createApp({
  
    data(){
        return {
            
        }
    }
})






// Mount the app to the DOM
app.mount('#app')
